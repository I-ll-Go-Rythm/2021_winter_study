#include <climits>
#include <iostream>
#include <stdio.h>
int main(void) {
	int a = 0, b = 0;
	printf("%d\n", INT_MAX);
	
	a = (INT_MAX - 10) % (INT_MAX/2) + (INT_MAX - 10) % (INT_MAX / 2);
	b = (INT_MAX - 10 + INT_MAX - 10) % INT_MAX;

	printf("%d %d \n", a, b);
	
	return 0;
}
